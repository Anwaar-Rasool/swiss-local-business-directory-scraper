# swiss-local-business-directory-scraper
Two-stage Python scraper for Switzerland's local business directory (local.ch) — collects listing URLs by category &amp; city, then extracts 22+ profile fields including contacts, hours, social links, and images using Playwright with stealth mode.
